{
  "name": "birknext-browser-companion",
  "version": "0.1.0",
  "private": true,
  "description": "BirkNext Browser Companion — Manifest V3 extension that reports safe page quality evidence from approved Target Environment pages to the local BirkNext backend.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "build": "node build.mjs",
    "build:notest": "node build.mjs --skip-tests"
  },
  "engines": {
    "node": ">=20"
  }
}
