mutation RejectSuggestion($input: RejectSuggestionInput!) {
    rejectSuggestion(input: $input) {
        success
        errors {
            code
            message
        }
        correlationId
    }
}
