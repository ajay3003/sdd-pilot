query GetQaDeltaReviews($projectId: String!) {
  qaDeltaReviews(projectId: $projectId) {
    id
    title
    projectId
    createdAt
    oldSpecFileName
    newSpecFileName
    analysisProfile
    summaryJson
  }
}
