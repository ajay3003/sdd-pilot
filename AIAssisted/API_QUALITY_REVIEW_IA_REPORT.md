API Quality Review information architecture — 2026-09-23

The change is confined to frontend presentation and tests. Request execution, authentication, read-only policy, findings, severity, persisted report enums and backend review logic are unchanged.

| Requested item | Audit and delivered result |
|---|---|
| A. Components audited | `ApiQualityReview.razor` (setup and all eight result tabs), its CSS, `ApiReviewTargetPicker`, `ApiReviewContractPanel`, `ApiReviewAccessDetails`, shared `ReviewDisclosure`, `ApiReviewPresentation`, presentation models, shared `ApiReviewContracts`, API review engine, OpenAPI finding/check factory, authentication gateway/context store, frontend page/semantics tests and engine tests. Coverage, limitations and read-only content are inline page sections, not separate components. |
| B. Repetition root cause | One details section already existed outside the tab switch. It was rendered under every selected tab. This was a placement/visibility problem, not eight duplicated implementations. Result summary and Key findings also intentionally remain shared. |
| C. Final owner | The existing page owns one post-run Review details section, rendered only on Overview. Every other tab has a descriptive return button that selects Overview and focuses its Review details heading. |
| D. Pre-run density | Kept Target, Review scope, API access, readiness and Run order. All six domain cards remain. Contracts carries the actual missing-evidence explanation; REST and GraphQL use short structural/schema limitation references. |
| E. Contract/REST/GraphQL cards | REST remains included with structural limitation. GraphQL remains included with schema limitation when prior selected-target evidence supports it. Schema retrieval is described as an attempt. Previous rejection explicitly says retrieval will be attempted again. Historical evidence is filtered to selected target IDs. |
| F. Memory-only help | Added: “Memory only means credentials are held only in the backend runtime for the current testing context and are not exposed to the review result.” Verified against the transient credential store and gateway. No credential fields were added. |
| G. Reviewed | Backend `Completed` maps to `Reviewed`, an execution state. Visible help: “Reviewed — available automated scope completed. This does not mean the service is fully validated.” Its badge is neutral rather than green. Partial, blocked and not-reviewed states remain distinct. |
| H. Old GraphQL state | The engine uses shared `ApiReviewCheckResult.NotApplicable` when introspection is rejected, with `Available=false` and `IntrospectionEnabled=false`. The old UI printed the check enum directly. There is no separate backend schema-availability enum. |
| I. Final GraphQL state | Contract availability reads the explicit availability fields and shows `Unavailable`, preserving the HTTP rejection reason. The compatibility projection also fixes saved reports. Backend enums are unchanged. Observed operations remain `Not tested` and appear on GraphQL as well as Contracts; the fixture retains 6 observed and 0/6 matched. |
| J. Availability versus drift | A missing REST contract yields no contract summary; REST structural comparisons can still emit `drift-shape` checks. GraphQL schema drift requires a retrieved schema. Successful comparisons are distinct from document/schema availability. |
| K. Contracts wording | Separate Contract availability and Drift comparison sections. A two-pass drift-only list reads “Drift checks · 2 completed · 0 changes detected.” Individual comparison rows say “No changes detected.” Coverage separates actual contract checks from drift/history checks. The exact live example was not fetched; the engine audit and browser fixture establish the two-response-shape-check case. Run count alone no longer implies a successful previous drift comparison. Retrieved metadata is collapsed; unavailable schema counts are not displayed as measured zeros. |
| L. Security observations | `sec-tls`: HTTPS observed, with explicit exclusion of protocol/cipher assessment. HSTS, Cache-Control and rate-limit headers: Header observed. Server disclosure absence: No issue detected. CORS preflight: Response observed, with HTTP-only/browser-enforcement boundary. CORS policy and introspection policy: Observed, with scope detail. `sec-xcto` retains Pass for the explicit nosniff-value criterion. Fail, Warning, Not tested and manual-review states are preserved. The no-findings-does-not-establish-security disclaimer remains. |
| M. Error checks | Leakage titles describe indicators; a successful indicator scan reads “No internal-detail indicators observed in the sampled error response.” Unknown-route status and invalid-query error-shape checks retain bounded Pass results. ProblemDetails warnings remain unchanged. |
| N. Performance thresholds | Captured policy defaults are warning >500 ms and poor >1000 ms. REST already included thresholds; GraphQL did not. Both now expose captured policy values. The backend-gateway timing disclaimer is unchanged. |
| O. Payload | The engine has an explicit `LargePayloadBytes` policy (default 1,048,576). Pass is retained with a recorded size and positive threshold, and the threshold is displayed. A missing size is presented as Not tested rather than the engine's zero-byte fallback; a Pass without a positive threshold is presented as Observed. Compression warnings remain; header presence is observational. |
| P. REST Result audit | `ApiReviewOperationResult.Result` is the worst review-check result, not the HTTP execution result. A successful HTTP request can therefore carry Warning. |
| Q. Operation display | Replaced Executed/Result ambiguity with Execution and Review checks. HTTP 2xx execution reads Successful; HTTP status, timing and bytes remain separate. Review warning/failed-check counts do not pretend to be source finding counts. Source findings remain inspectable below. |
| R. Finding model | `ApiReviewFinding` has source ID, target, display Check, endpoint, severity, description and evidence. No logical-issue/grouping model or dedicated semantic rule key exists. The factory embeds a rule prefix and runtime hash in the source ID; that serialized ID format is not a grouping contract. |
| S. Grouping decision | No logical grouping added. No title matching, ID-prefix parsing or host-only deduplication. This follows the requested conservative fallback. |
| T. Counts | All source findings and severity counts are preserved. Service/table headings and the all-findings action explicitly say source findings. No logical-issue count is invented. The browser fixture retains nine source findings. |
| U. Key findings | Existing severity ordering and five-item cap retained, including separate service evidence. The action now reads All source findings. Repeated titles remain because there is no supported logical grouping key. |
| V. Global details | Coverage; Review limitations with nested technical limitations; read-only scope; captured API targets; authentication details. Contract availability/results belong to Contracts, not a repeated configuration panel. Manual obligations remain visible on Overview before deep details, with “not findings” text. Post-run target selection is disabled and uses the report's captured targets. |
| W. Authentication/CDP | The stale text comes from the shared gateway capability Reason, not Browser Companion. API QR now presents available API capability as API access through Local HTTPS Proxy and explicitly states that browser runtime inspection is outside this review. It does not infer browser authentication verification or claim CDP is the only browser architecture. Unavailable capability reasons remain. Post-run fields use recorded report capabilities. |
| X. Responsive | Fixed the narrow-screen setup-grid override and child-component scoped-style boundary. Tables retain readable minimum widths and scroll inside focusable regions. Grid tracks can shrink. Long names/routes retain full text and wrap. Positioned table containers prevent hidden header text from causing document overflow. Tested 1440, 768 and 390 px. |
| Y. Accessibility | Existing tab selected state, arrow navigation, scoped headers and disclosure `aria-expanded` retained. Added keyboard-focusable table regions, descriptive finding evidence labels, return-to-details focus, and visible Reviewed help. Status/severity remains text, not color alone. |
| Z. Focused frontend | 120 passed, 0 failed. Filter: `FullyQualifiedName~ApiReview|FullyQualifiedName~ApiQualityReview`. |
| AA. Focused backend | Backend unchanged; extra regression check: 19 API review engine tests passed. |
| AB. Full frontend | 4,038 passed, 0 failed. The initial workspace baseline was 3,999 passed/1 unrelated Browser Discovery copy failure; other sessions completed their own unrelated changes during this task. |
| AC. Full backend | Backend unchanged. Baseline outside sandbox: 2,612 passed, 6 explicitly skipped, 0 failed. Initial sandbox runs encountered Windows certificate/local-runtime restrictions. Isolated in-repository build output avoided the running API executable lock and preserved source-path-dependent tests. |
| AD. Debug builds | Frontend and backend project builds passed, 0 errors. Existing warnings remain. |
| AE. Release builds | Frontend and backend project builds passed, 0 errors. Existing warnings remain. |
| AF. Manual pre-run visual review | Screenshots inspected; four-card hierarchy and six domains retained; target operations collapsed by default; expanded target/auth details checked for overflow. |
| AG. Manual post-run visual review | Screenshots inspected for Overview, Contracts and narrow GraphQL. Automated browser interaction covered all eight tabs at all three widths, expanded details, unavailable schema text, details-return focus and absence of page errors/page-wide overflow. API responses were mocked; this is UI verification, not real authenticated target execution. |
| AH. Files | Page/CSS; target and authentication components; presentation models and service; new evidence-presentation helper; three updated API QR test files; two new API QR test files; this report. No backend/shared-contract files changed. |
| AI. Commit | See the commit containing this report and the session completion message for its hash. |
| AJ. Git status | Only this task's named files are staged for the commit. Unrelated Browser Discovery/Frontend Quality Review work was preserved and committed by its owners. Final status is reported after this commit. |
| AK. Remaining limitations | Logical grouping is intentionally absent. Raw engine enums and HTML export retain existing semantics; the new wording is an interactive UI projection. Real target authentication/introspection was not re-executed. The six backend integration/real-environment tests remain skipped. This is a focused responsive/accessibility pass, not a full accessibility certification. |

Verification artifacts are in the workspace root: `apiqr-focused-frontend.log`, `apiqr-full-frontend.log`, `apiqr-focused-backend.log`, `apiqr-baseline-backend-unrestricted.log`, `apiqr-debug-*.log`, `apiqr-release-*.log`, `apiqr-browser-check.cjs`, `apiqr-browser-result.json`, and `apiqr-pre-*.png` / `apiqr-post-*.png`. The browser fixture uses example.test hosts and contains no credentials. `git diff --check` passed; the UI source scan found no affirmative Secure/Fully validated/Contract passed/GraphQL passed/Schema passed/All good/Compliant claims.
