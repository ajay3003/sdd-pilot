# Local PostgreSQL defaults used by docker-compose.yml and scripts/start-local.ps1.
# If these values change after the postgres_data volume already exists, recreate
# the local database volume or update ConnectionStrings__Default to match it.
POSTGRES_DB=birknext
POSTGRES_USER=birknext
POSTGRES_PASSWORD=birknext
