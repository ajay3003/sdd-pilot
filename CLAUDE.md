<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, local container runtime, and other important information,
read the current plan at `specs/008-traceability-first/plan.md`.

Local development environment notes:
- Podman is used instead of Docker
- Use `podman compose` instead of `docker compose`
- Docker-compatible compose files remain valid
<!-- SPECKIT END -->